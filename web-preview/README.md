# PocketAgent Web Preview

This static preview mirrors the v0.2.4 mobile document-search direction: chat surface, left history drawer, search authorization sheet, and candidate document cards.

It cannot open Android system pickers or read real phone files. Install the APK to test actual Storage Access Framework permissions and document indexing.